[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tda_diffusion"
version = "0.1.0"
description = "TDA observables of hydrodynamic normal-mode height profiles"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools]
packages = ["src"]